# Environment Variables for BillClaw Connect Service
#
# This file demonstrates available environment variables.
# For production use, consider using ~/.firela/billclaw/config.json instead.
#
# Config file example (~/.firela/billclaw/config.json):
# {
#   "version": 1,
#   "connect": {
#     "port": 4456,
#     "host": "localhost",
#     "publicUrl": "https://billclaw.yourdomain.com"
#   },
#   "plaid": { "clientId": "xxx", "secret": "xxx", "environment": "sandbox" },
#   "gmail": { "clientId": "xxx", "clientSecret": "xxx" }
# }

# Server Configuration
PORT=4456
HOST=localhost

# Public URL (for production OAuth callbacks)
# Required for production use with real bank authentication
# Examples:
# - https://billclaw.yourdomain.com (your domain)
# - https://abc123.ngrok.io (ngrok tunnel for testing)
PUBLIC_URL=https://billclaw.yourdomain.com

# TLS/SSL Configuration (optional, for HTTPS)
# TLS_ENABLED=false
# TLS_KEY_PATH=/path/to/tls/key.pem
# TLS_CERT_PATH=/path/to/tls/cert.pem

# Plaid API Credentials
# Get these from: https://dashboard.plaid.com/
PLAID_CLIENT_ID=your_client_id_here
PLAID_SECRET=your_secret_here
PLAID_ENVIRONMENT=sandbox

# Gmail OAuth Credentials (optional)
GMAIL_CLIENT_ID=your_gmail_client_id
GMAIL_CLIENT_SECRET=your_gmail_client_secret
